# Estado de GitHub Pages

GitHub Pages está configurado para publicar mediante **GitHub Actions** desde `main`.

Este archivo se añadió después de activar `Settings → Pages → Source: GitHub Actions` para disparar una ejecución nueva del workflow y validar el despliegue con la configuración ya habilitada.
